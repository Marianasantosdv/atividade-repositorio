const btn = document.getElementById("btnSurpresa");
const mensagem = document.getElementById("mensagem");

btn.addEventListener("click", () => {
    mensagem.textContent = "Esse é o meu primeiro projeto no github!";
});
